#!/usr/bin/env python3
"""Extract cross-harness normalized traces into external local storage."""

from pathlib import Path
import sys


sys.path.insert(0, str(Path(__file__).resolve().parents[1]))

from retro_eval.pipeline import main  # noqa: E402


if __name__ == "__main__":
    raise SystemExit(main())
