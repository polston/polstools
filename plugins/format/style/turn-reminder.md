Format reminder for this reply: begin with the literal line `# FINDINGS`, then
`# PROBLEMS` (bold `**N.**` / `**N.1.**` prefixed lines, not markdown lists),
then `# ASKS`, then `---`, then everything else. All three sections present
even when a section's content is `1. None.`
