{
  "description": "Injects the response-format spec at session start and a short reminder every turn (per-turn reinjection counters within-session drift). Plain stdout becomes context; no wrapper scripts and no exit-0 masking, so a missing payload surfaces as a failed hook.",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "cat \"${CLAUDE_PLUGIN_ROOT}/style/response-format.md\""
          }
        ]
      }
    ],
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "cat \"${CLAUDE_PLUGIN_ROOT}/style/turn-reminder.md\""
          }
        ]
      }
    ]
  }
}
